# 华理教育管理系统

## 环境配置

本项目使用 Python 3.10 环境，可以通过以下方式配置：

1. 使用 conda 创建环境：
```bash
conda env create -f environment.yml
```

2. 激活环境：
```bash
conda activate py310
```

或者直接运行：
```bash
source activate.sh
```
