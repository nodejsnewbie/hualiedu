# direnv 配置文件
# 安装 direnv: https://direnv.net/
# 使用方法: 在项目目录下运行 `direnv allow`

# 自动激活 conda py313 环境
layout anaconda py313

# 或者如果上面的命令不工作，使用这个：
# eval "$(conda shell.bash hook)"
# conda activate py313
